  

const config = {
  appDirectory: "app",
  assetsBuildDirectory: "public/build",
  publicPath: "/build/",
  serverBuildPath: "build/server.js", // Output directory for the server
  devServerPort: 8002,
};

export default config;